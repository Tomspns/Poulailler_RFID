<Window x:Class="RFIDwpf.AddEditPouleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion des Poules" Height="450" Width="550"
        WindowStartupLocation="CenterScreen"
        MinHeight="450" MinWidth="550"
        Background="#F8F9FA"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Animation d'ouverture -->
        <Storyboard x:Key="WindowLoadAnimation">
            <DoubleAnimation Storyboard.TargetName="MainCard"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5"
                             BeginTime="0:0:0.1">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainCard"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="30" To="0" Duration="0:0:0.5"
                             BeginTime="0:0:0.1">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Styles TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <!-- Assure la visibilité du texte -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                  Margin="{TemplateBinding Padding}"
                                  Foreground="{TemplateBinding Foreground}"/>
                            <!-- IMPORTANT -->
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#FF6B35" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#E0E0E0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF6B35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styles Button -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowLoadAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <!-- En-tête -->
        <Rectangle Height="120" VerticalAlignment="Top">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF6B35" Offset="0"/>
                    <GradientStop Color="#F7931E" Offset="0.5"/>
                    <GradientStop Color="#FDC830" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <StackPanel VerticalAlignment="Top" Margin="0,25,0,0">
            <TextBlock Text="🐔" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="Gestion des Poules" FontFamily="Segoe UI" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Card principale -->
        <Border x:Name="MainCard" Background="White" CornerRadius="20" Margin="30,140,30,30" Padding="35" Opacity="0">
            <Border.RenderTransform>
                <TranslateTransform Y="30"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Direction="270" ShadowDepth="5" BlurRadius="20" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel>
                <!-- ID Poule -->
                <Border Background="#FFF3E0" CornerRadius="12" Padding="20,15" Margin="0,0,0,20">
                    <StackPanel>
                        <Label Content="🆔 ID Poule" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Foreground="#FF6B35" Padding="0" Margin="0,0,0,8"/>
                        <TextBox x:Name="IdPouleTextBox" Background="White" Foreground="Black" Height="45"/>
                    </StackPanel>
                </Border>

                <!-- Nom Poule -->
                <Border Background="#E8F5E9" CornerRadius="12" Padding="20,15" Margin="0,0,0,25">
                    <StackPanel>
                        <Label Content="📝 Nom de la Poule" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50" Padding="0" Margin="0,0,0,8"/>
                        <TextBox x:Name="NomTextBox"  Background="White" Foreground="Black" Height="45"/>
                    </StackPanel>
                </Border>

                <!-- Race Poule -->
                <Border Background="#FFFDE7" CornerRadius="12" Padding="20,15" Margin="0,0,0,25">
                    <StackPanel>
                        <Label Content="🐓 Race de la Poule" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Foreground="#FFC107" Padding="0" Margin="0,0,0,8"/>
                        <TextBox x:Name="RaceTextBox" Background="White" Foreground="Black" Height="45"/>
                    </StackPanel>
                </Border>


                <!-- Boutons -->
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="➕ Ajouter" Style="{StaticResource ModernButton}" Background="#4CAF50" Foreground="White" Height="50" Click="Ajouter_Click"/>
                    <Button Grid.Column="2" Content="✏️ Modifier" Style="{StaticResource ModernButton}" Background="#2196F3" Foreground="White" Height="50" Click="Modifier_Click"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
